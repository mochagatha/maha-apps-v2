import 'package:flutter/widgets.dart';

class StatementLettersProvider extends ChangeNotifier {
  Future<String?> submitStatementLetter() async {
    // implement submit
    await Future.delayed(Duration(seconds: 1));
    return null;
  }

  Future<String?> submitStatementLetterSignature() async {
    // implement submit
    await Future.delayed(Duration(seconds: 1));
    return null;
  }
}